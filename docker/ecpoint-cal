#!/bin/sh

image="$1"
if [ -z "$image" ] ; then
  image="ecmwf/ecpoint-pycal:latest"
fi

exec docker run \
     --rm \
     --read-only \
     --user $(id -u):$(id -g) \
     -e DISPLAY=$DISPLAY \
     -e HOME=$HOME \
     -v /home:/home \
     -v /hugetmp:/hugetmp \
     -v /perm:/perm \
     -v /scratch:/scratch \
     -v /tmp:/tmp \
     -v /var/tmp:/var/tmp \
     $image
